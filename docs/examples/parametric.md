# 参数化建模

使用程序化生成创建复杂几何体。

## 花瓶

利用 bmesh 创建参数化花瓶，通过旋转曲线生成优雅轮廓。

## 塔楼

参数化生成多层塔楼，带渐变收分和装饰元素。

## 对话示例

```
> 生成一个 8 层塔楼，底半径 2.5，收缩率 0.92
> 创建一个程序化花瓶，32 分段，16 环
```

## 完整脚本

查看 `examples/parametric_objects.py` 和 `src/scripts/procedural_gen.py`。